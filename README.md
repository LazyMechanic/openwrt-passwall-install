Script to install package `luci-app-passwall2`, `xray-core` and all dependencies

Single line install:
```
wget -O /tmp/passwall_install.sh https://example.com/install.sh && sh /tmp/passwall_install.sh --verbose
```

Special Thanks to https://github.com/amirhosseinchoghaei/Passwall